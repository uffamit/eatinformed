// Firestore rules have been cleared as Firebase integration is reverted.
// To re-integrate Firestore:
// 1. Set up Firestore in your Firebase project.
// 2. Define security rules here. For example:
/*
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Example: Users collection
    // match /users/{userId} {
    //   allow read, write: if request.auth != null && request.auth.uid == userId;
    // }

    // Example: Usernames collection (for unique username checks)
    // match /usernames/{username} {
    //   allow read: if true; // Allows anyone to check if a username exists
    //   allow create: if request.auth != null && request.resource.data.uid == request.auth.uid;
    // }
  }
}
*/
